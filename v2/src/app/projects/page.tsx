import PolygonButton from '@/components/ui/polygon-button';

const Home = () => {
  return (
    <section className="container flex flex-col items-center justify-center">
      <PolygonButton size="small" />
    </section>
  );
};

export default Home;
